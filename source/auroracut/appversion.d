module auroracut.appversion;

/** Public application identity used by the title bar, logs, and CLI. */
enum appName = "Aurora Cut";
enum appVersion = "0.13.5";
enum appDisplayName = appName ~ " " ~ appVersion;
